import React from "react";
import { View, Text } from "react-native";

export default function VideosScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Videos screen (next)</Text>
    </View>
  );
}
